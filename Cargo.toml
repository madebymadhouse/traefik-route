[package]
name = "traefik-route"
version = "0.1.0"
edition = "2021"
description = "Manage Traefik custom domain routes for Coolify-hosted apps"
license = "MIT"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
anyhow = "1"
